# ===========================================
# Pilot Profile Configuration
# ===========================================
# Ativado com: -Dspring.profiles.active=pilot ou spring.profiles.active=pilot
#
# Este profile é usado para o primeiro cliente real (piloto).
# Ele herda as configs de application.properties e sobrescreve
# apenas o necessário.

# Evolution API Configuration
evolution.api.base-url=http://localhost:8082
evolution.api.instance-name=troquim-pilot
evolution.api.api-key=troquim237